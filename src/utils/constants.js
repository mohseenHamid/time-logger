// Time Logger Constants

// Fuzzy search scoring thresholds
export const FUZZY_SCORE = {
  EXACT_MATCH: 100,
  SUBSTRING_MATCH_BASE: 80,
  SUBSTRING_BONUS: 10,
  TOKEN_OVERLAP_MULTIPLIER: 7,
  PREFIX_MATCH_BONUS: 10,
  MINIMUM_MATCH_THRESHOLD: 60,
}

// UI timing constants (milliseconds)
export const UI_TIMING = {
  DROPDOWN_BLUR_DELAY: 120,
  QUICK_ENTRY_BLUR_DELAY: 150,
  AUTO_FOCUS_DELAY: 100,
}

// Display limits
export const DISPLAY_LIMITS = {
  DROPDOWN_MAX_ITEMS: 8,
  QUICK_ENTRY_MAX_ITEMS: 5,
}
